# docker-compose.yml
version: "3.8"

services:
  profile-sbi:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: profile-sbi-app
    ports:
      - "5173:80"
    restart: unless-stopped
